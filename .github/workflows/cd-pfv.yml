name: CD — PFV (Pre-Production)

# Triggered on push to staging branch
on:
  push:
    branches: [staging]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual PFV deployment'
        required: true
        default: 'Manual validation deployment'

concurrency:
  group: deploy-pfv
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  id-token: write

env:
  AWS_REGION: us-east-1
  ECS_CLUSTER: staging-imposter-cluster
  API_SERVICE: staging-imposter-api
  WEB_SERVICE: staging-imposter-web
  API_TASK_FAMILY: staging-imposter-api
  WEB_TASK_FAMILY: staging-imposter-web

jobs:
  # ─────────────────────────────────────────
  # 1. Build & push Docker images tagged :pfv
  # ─────────────────────────────────────────
  build-push:
    name: Build & Push (pfv)
    runs-on: ubuntu-latest
    outputs:
      image_tag: pfv-${{ github.sha }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: Set lowercase repo name
        run: echo "REPO_LC=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ghcr.io/${{ env.REPO_LC }}/api:pfv
            ghcr.io/${{ env.REPO_LC }}/api:pfv-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push Web
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ghcr.io/${{ env.REPO_LC }}/web:pfv
            ghcr.io/${{ env.REPO_LC }}/web:pfv-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─────────────────────────────────────────
  # 2. Deploy to ECS (staging/pfv)
  # ─────────────────────────────────────────
  deploy:
    name: Deploy to PFV (ECS)
    runs-on: ubuntu-latest
    needs: build-push
    environment: pfv
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update API task definition
        id: api-task-def
        run: |
          IMAGE_TAG="${{ needs.build-push.outputs.image_tag }}"

          aws ecs describe-task-definition \
            --task-definition ${{ env.API_TASK_FAMILY }} \
            --query 'taskDefinition' \
            --output json | \
          jq --arg TAG "$IMAGE_TAG" \
            '.containerDefinitions[0].image = "ghcr.io/shakayz/guess-who/api:" + $TAG' | \
          jq 'del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)' \
            > /tmp/api-task-def.json

          NEW_ARN=$(aws ecs register-task-definition \
            --cli-input-json file:///tmp/api-task-def.json \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          echo "arn=${NEW_ARN}" >> $GITHUB_OUTPUT
          echo "New API task: ${NEW_ARN}"

      - name: Update Web task definition
        id: web-task-def
        run: |
          IMAGE_TAG="${{ needs.build-push.outputs.image_tag }}"

          aws ecs describe-task-definition \
            --task-definition ${{ env.WEB_TASK_FAMILY }} \
            --query 'taskDefinition' \
            --output json | \
          jq --arg TAG "$IMAGE_TAG" \
            '.containerDefinitions[0].image = "ghcr.io/shakayz/guess-who/web:" + $TAG' | \
          jq 'del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)' \
            > /tmp/web-task-def.json

          NEW_ARN=$(aws ecs register-task-definition \
            --cli-input-json file:///tmp/web-task-def.json \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          echo "arn=${NEW_ARN}" >> $GITHUB_OUTPUT
          echo "New Web task: ${NEW_ARN}"

      - name: Deploy ECS services
        run: |
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.API_SERVICE }} \
            --task-definition ${{ steps.api-task-def.outputs.arn }} \
            --no-cli-pager > /dev/null

          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.WEB_SERVICE }} \
            --task-definition ${{ steps.web-task-def.outputs.arn }} \
            --no-cli-pager > /dev/null

          echo "ECS services updated."

      - name: Wait for services to stabilize
        run: |
          echo "Waiting for ECS services to stabilize..."
          aws ecs wait services-stable \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.API_SERVICE }} ${{ env.WEB_SERVICE }}
          echo "PFV deployed — SHA: ${{ github.sha }}"

      - name: Notify deployment status
        if: always()
        run: echo "PFV Deployment ${{ job.status }} for SHA ${{ github.sha }}"
